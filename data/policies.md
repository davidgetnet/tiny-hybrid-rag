# Acorn Labs Deployment Policies

Every production deployment requires approval. Alice can approve production deployments for Backend services, while Ben can approve production deployments for Frontend services.

Security-sensitive deployments require an additional review from Clara. Every production deployment must be recorded in the deployment log.

Emergency deployments still require approval, but the deployment log may be completed immediately after deployment.
